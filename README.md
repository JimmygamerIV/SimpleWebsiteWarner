# SimpleWebsiteWarner
This Chrome extension simply allows you to edit a blacklisted domains list (configurable in the background.js file). If any of the domains in the list is visited, a notification will be sent advising this
